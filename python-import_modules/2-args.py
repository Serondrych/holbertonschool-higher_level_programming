#!/usr/bin/python3
import sys
if __name__ = '__main__':
    num = len(sys.argv)
    if num == 1:
        print("{} arguments".format(num - 1)
    elif num == 2:
        print("{} argument:".format(num - 1)
    else:
        print("{} arguments:".format(num - 1)

    for n in range(1, number):
        print("{}: {}".format(1, sys.argv([n]))
